# Supabase
.branches
.temp

# Edge Function secrets (auto-loaded by `supabase functions serve` locally).
# Never commit — Google Places API key + alert webhook live here.
.env
.env.local
.env.*.local

# dotenvx
.env.keys
